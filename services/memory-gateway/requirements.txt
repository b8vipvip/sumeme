fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
httpx>=0.28,<1
pydantic-settings>=2.8,<3
anyio>=4.8,<5
PyJWT[crypto]>=2.10,<3
# Pin the current generated SDK used by the Letta 0.16.x API. The previous <1
# cap installed a legacy client against the current server and obscured agent
# creation failures behind response-model incompatibilities.
letta-client==1.12.1
